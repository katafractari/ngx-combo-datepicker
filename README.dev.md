Publish to NPM
```
npm install
npm run packagr
npm publish dist
```

Create and install tarball
```
npm install
npm run packagr
cd dist
npm pack
cd project_using_combodatepicker
npm install path_to_tarball.tgz
```
